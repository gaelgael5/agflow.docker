// VaultUnlockDialog supprimé — remplacé par Harpocrate (server-side).
// Stub de compatibilité pour les pages qui importent encore ce composant.

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function VaultUnlockDialog(_props: {
  open: boolean;
  email: string;
  onComplete: () => void;
  onClose?: () => void;
}) {
  return null;
}
