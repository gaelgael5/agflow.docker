# Configuration du déploiement distant
# Copier ce fichier vers scripts/.env.remote-deploy et remplir les valeurs.
# NE JAMAIS committer scripts/.env.remote-deploy — il contient des secrets.

# Cible SSH
REMOTE_HOST=192.168.10.xxx
REMOTE_PORT=22
REMOTE_USER=root

# Authentification — choisir UNE méthode :

# Option A : clé SSH / certificat (recommandé)
REMOTE_KEY=/chemin/vers/cle-privee.pem
# REMOTE_PASSWORD=

# Option B : mot de passe (nécessite sshpass installé sur la machine locale)
# REMOTE_KEY=
# REMOTE_PASSWORD=mot-de-passe-ici

# Répertoire du projet sur la machine distante
REMOTE_DIR=/opt/agflow.docker

# Fichier compose à utiliser
COMPOSE_FILE=docker-compose.dev.yml

# Nombre de lignes de logs affichées après le démarrage
LOG_LINES=80
